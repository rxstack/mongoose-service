version: '3'

services:
    app:
        tty: true
        build:
            context: ./app
        volumes:
            - ${APP_PATH}:/var/www/application
        links:
            - mongo
    mongo:
        image: mongo
        ports:
            - 27017:27017


